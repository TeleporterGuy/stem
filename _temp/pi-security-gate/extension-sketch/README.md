# security-gate (starter)

Copy this whole directory to:

```text
~/.pi/agent/extensions/security-gate/
```

Then finish wiring imports to match **your** Pi package names
(`@earendil-works/pi-coding-agent` vs older `@mariozechner/...`). Check what
other loaded extensions import.

Reload Pi config in Pendant after install.
