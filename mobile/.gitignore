# Expo and native build output. `ios/` and `android/` are absent on purpose:
# this is a managed project, and the native projects are generated (`npx expo
# prebuild`) rather than kept.
.expo/
dist/
web-build/
ios/
android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
expo-env.d.ts
